input {
	tcp {
		port => 5000
	}
}

filter {
  grok {
    match => { "message" => "%{S3_ACCESS_LOG}"}
  }

  date {
    locale => "en"
    match => ["timestamp", "d/MMM/YYYY:HH:mm:ss Z"]
  }

}

output {
	elasticsearch {
		hosts => "elasticsearch:9200"
	}
}
